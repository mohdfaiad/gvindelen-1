CREATE TABLE OBJTYPEATTR_REF (
    ATTR_CODE  SIGN_ATTR NOT NULL /* SIGN_ATTR = VARCHAR(30) */,
    KEI        CODE_KEI NOT NULL /* CODE_KEI = NUMERIC(3,0) */,
    ATTR_NAME  NAME_ATTR NOT NULL /* NAME_ATTR = VARCHAR(255) */,
    ATTR_LEN   LEN_ATTR NOT NULL /* LEN_ATTR = NUMERIC(6,0) NOT NULL */
);
ALTER TABLE OBJTYPEATTR_REF ADD CONSTRAINT PK_OBJTYPEATTR_REF PRIMARY KEY (ATTR_CODE);
ALTER TABLE OBJTYPEATTR_REF ADD CONSTRAINT FK_OBJTYPEATTRREF2KEI FOREIGN KEY (KEI) REFERENCES KEI_OKP (KEI) ON UPDATE CASCADE;
