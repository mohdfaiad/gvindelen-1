CREATE TABLE QUERIES (
    QUERY_SIGN  SIGN_ATTR NOT NULL /* SIGN_ATTR = VARCHAR(30) */,
    QUERY_TEXT  VALUE_ATTR /* VALUE_ATTR = VARCHAR(4000) */
);
ALTER TABLE QUERIES ADD CONSTRAINT PK_QUERIES PRIMARY KEY (QUERY_SIGN);