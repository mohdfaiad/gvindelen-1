CREATE TABLE DOCFORM_REF (
    DOCFORM      SIGN_DOCFORM /* SIGN_DOCFORM = VARCHAR(10) NOT NULL */,
    ORIENTATION  SIGN_ORIENTATION NOT NULL /* SIGN_ORIENTATION = CHAR(1) */,
    OBJTYPE      SIGN_OBJTYPE NOT NULL /* SIGN_OBJTYPE = VARCHAR(10) */
);
ALTER TABLE DOCFORM_REF ADD CONSTRAINT PK_DOCFORMS PRIMARY KEY (DOCFORM);
ALTER TABLE DOCFORM_REF ADD CONSTRAINT FK_DOCFORMS_OBJTYPE FOREIGN KEY (OBJTYPE) REFERENCES OBJTYPE_REF (OBJTYPE) ON UPDATE CASCADE;
