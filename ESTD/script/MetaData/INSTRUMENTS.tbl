CREATE TABLE INSTRUMENTS (
    INSTR_ID  ID_OBJECT NOT NULL /* ID_OBJECT = INTEGER */
);
ALTER TABLE INSTRUMENTS ADD CONSTRAINT PK_INSTRUMENTS PRIMARY KEY (INSTR_ID);
ALTER TABLE INSTRUMENTS ADD CONSTRAINT FK_INSTRUMENT2OBJECT FOREIGN KEY (INSTR_ID) REFERENCES OBJECTS (OBJ_ID) ON DELETE CASCADE ON UPDATE CASCADE;
