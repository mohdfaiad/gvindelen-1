CREATE TABLE BLOCK_REF (
    BLOCK_SIGN  SIGN_BLOCK NOT NULL /* SIGN_BLOCK = VARCHAR(10) */,
    LITERA      SIGN_LITERA /* SIGN_LITERA = CHAR(1) */
);
ALTER TABLE BLOCK_REF ADD CONSTRAINT PK_BLOCK_REF PRIMARY KEY (BLOCK_SIGN);