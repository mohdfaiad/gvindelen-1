CREATE GLOBAL TEMPORARY TABLE TMP_PARAMS (
    PARAM_NAME   SIGN_ATTR NOT NULL /* SIGN_ATTR = VARCHAR(30) */,
    PARAM_VALUE  VALUE_ATTR /* VALUE_ATTR = VARCHAR(4000) */
) ON COMMIT DELETE ROWS;
ALTER TABLE TMP_PARAMS ADD CONSTRAINT PK_TMP_PARAMS PRIMARY KEY (PARAM_NAME);