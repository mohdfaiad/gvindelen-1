CREATE TABLE EQUIPMENT_TREE (
    OBJ_ID        ID_OBJECT NOT NULL /* ID_OBJECT = INTEGER */,
    OBJ_CHILD_ID  ID_OBJECT NOT NULL /* ID_OBJECT = INTEGER */
);
ALTER TABLE EQUIPMENT_TREE ADD CONSTRAINT PK_EQUIPMENT_TREE PRIMARY KEY (OBJ_ID, OBJ_CHILD_ID);
ALTER TABLE EQUIPMENT_TREE ADD CONSTRAINT FK_EQUIPTREE2EQUIPMENT FOREIGN KEY (OBJ_ID) REFERENCES EQUIPMENTS (EQUIP_ID) ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE EQUIPMENT_TREE ADD CONSTRAINT FK_EQUIPTREECHILD2EQUIPMENT FOREIGN KEY (OBJ_CHILD_ID) REFERENCES EQUIPMENTS (EQUIP_ID) ON DELETE CASCADE ON UPDATE CASCADE;
