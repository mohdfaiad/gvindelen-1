CREATE TABLE BAND_REF (
    BAND_SIGN  SIGN_BAND /* SIGN_BAND = VARCHAR(4) NOT NULL */,
    BAND_NAME  NAME_OBJECT NOT NULL /* NAME_OBJECT = VARCHAR(255) */
);
ALTER TABLE BAND_REF ADD CONSTRAINT PK_BAND_REF_1 PRIMARY KEY (BAND_SIGN);