CREATE TABLE CELLS (
    BLOCK_SIGN  SIGN_BLOCK NOT NULL /* SIGN_BLOCK = VARCHAR(10) */,
    SIZE_CELL   SIZE_MM NOT NULL /* SIZE_MM = DECIMAL(6,1) NOT NULL */,
    SIZE_FONT   VALUE_INTEGER NOT NULL /* VALUE_INTEGER = INTEGER */
);
ALTER TABLE CELLS ADD CONSTRAINT PK_CELLS PRIMARY KEY (BLOCK_SIGN);