CREATE TABLE DETINSET (
    DOCSET_ID  ID_DOCSET NOT NULL /* ID_DOCSET = INTEGER */,
    DETAIL_ID  ID_OBJECT NOT NULL /* ID_OBJECT = INTEGER */
);
ALTER TABLE DETINSET ADD CONSTRAINT PK_DETINSET PRIMARY KEY (DOCSET_ID, DETAIL_ID);
ALTER TABLE DETINSET ADD CONSTRAINT FK_DET2DETINSET FOREIGN KEY (DETAIL_ID) REFERENCES DETAILS (DETAIL_ID) ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE DETINSET ADD CONSTRAINT FK_DETINSET2SUBJSET FOREIGN KEY (DOCSET_ID) REFERENCES DOCSETS (DOCSET_ID) ON DELETE CASCADE ON UPDATE CASCADE;
