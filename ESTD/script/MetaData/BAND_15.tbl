CREATE TABLE BAND_15 (
    LINE_ID  ID_LINE NOT NULL /* ID_LINE = INTEGER */,
    NOTE     TEXT /* TEXT = VARCHAR(255) */
);
ALTER TABLE BAND_15 ADD CONSTRAINT PK_BAND_15 PRIMARY KEY (LINE_ID);
ALTER TABLE BAND_15 ADD CONSTRAINT FK_BAND_15_LINE FOREIGN KEY (LINE_ID) REFERENCES LINES (LINE_ID) ON DELETE CASCADE ON UPDATE CASCADE;
