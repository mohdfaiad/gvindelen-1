CREATE TABLE DETECT_OBJECTCODE (
    LABEL_MASK  LABEL_OBJECT NOT NULL /* LABEL_OBJECT = VARCHAR(25) */,
    OBJTYPE     SIGN_OBJTYPE NOT NULL /* SIGN_OBJTYPE = VARCHAR(10) */,
    CODE_MASK   MASK_VALUE /* MASK_VALUE = VARCHAR(100) */,
    MASK_LEVEL  VALUE_INTEGER NOT NULL /* VALUE_INTEGER = INTEGER */
);
ALTER TABLE DETECT_OBJECTCODE ADD CONSTRAINT PK_DETECT_OBJECTCODE PRIMARY KEY (LABEL_MASK);
ALTER TABLE DETECT_OBJECTCODE ADD CONSTRAINT FK_DETECTLBLCODE2OBJECTTYPE FOREIGN KEY (OBJTYPE) REFERENCES OBJTYPE_REF (OBJTYPE) ON DELETE CASCADE ON UPDATE CASCADE;
